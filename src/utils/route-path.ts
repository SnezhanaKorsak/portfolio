export enum RoutePath {
    //start = "/start",
    navigation = "/navigation",
    about = "/about",
    skills = "/skills",
    projects = "/projects",
    contacts = "/contacts",
    notFound = "/404",
}